"""Application interface layer (Phase 7): mocked sessions, HTTP surface and
the app core wiring the agent runtime to the UI. No framework — ADR-007."""

from .app import ParcelPilotApp

__all__ = ["ParcelPilotApp"]
